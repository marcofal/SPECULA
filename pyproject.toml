[build-system]
requires = ["setuptools>=61.0", "wheel", "setuptools_scm>=8.0"]
build-backend = "setuptools.build_meta"

[project]
name = "specula"
description = "Scalable Parallel Execution of Computations Upscaling Large Adaptive optics simulations"
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Fabio Rossi", email = "fabio.rossi@inaf.it" },
    { name = "Alfio Puglisi" },
    { name = "Guido Agapito" },
    { name = "INAF Arcetri Adaptive Optics group" }
]
keywords = ["Adaptive Optics", "Astrophysics", "INAF", "Arcetri"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
]
dynamic = ["version", "dependencies"]

[project.scripts]
specula_frontend_start = "specula.scripts.web_frontend:start"
specula_frontend_stop = "specula.scripts.web_frontend:stop"
specula = "specula.scripts.specula_main:main"

[tool.setuptools]
packages = [
    "specula",
    "specula.data_objects",
    "specula.processing_objects"
]
include-package-data = true

[tool.setuptools.dynamic]
dependencies = { file = "requirements.txt" }

[project.optional-dependencies]
control = ["control"]
diagrams = ["orthogram"]
all = ["control", "orthogram"]

[tool.setuptools_scm]
write_to = "specula/_version.py"
